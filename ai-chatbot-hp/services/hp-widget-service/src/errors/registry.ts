import { ErrorDetail } from '@hp-intelligence/core';

/**
 * Atomic Registry for Widget Service.
 * Local service-specific errors can be defined here and merged with the core.
 */
export const WidgetErrorRegistry: Record<string, ErrorDetail> = {
  // Service-Local logic can be added here
};
